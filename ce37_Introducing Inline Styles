
class Colorful extends React.Component {
  render() {
    return (
      <div style={{color: "red", fontSize: 72}}>Big Red</div>
    );
  }
};

//Inline styles in JSX must be written as objects instead of strings
//kebab-case properties aren't accepted so you have to make them camelCase
